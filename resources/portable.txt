このファイルが存在する場合、MeeMee Screenerはポータブルモードで動作します。

ポータブルモード:
- データは実行ファイルと同じフォルダの "data" サブフォルダに保存されます
- USBメモリなどに入れて持ち運ぶことができます

通常モード:
- このファイルを削除すると、通常モードに戻ります
- データは %LOCALAPPDATA%\MeeMeeScreener に保存されます

注意:
- モードを切り替えると、データの保存場所が変わります
- 既存のデータは自動的には移行されません
