# Cleanup Manifest 2026-04-03

## Scope

This pass only removes generated artifacts, cache trees, and screenshots/logs that are not part of MeeMee runtime behavior.

## Deleted categories

- `output/analysis/`
  - Generated by `scripts/generate_tracking_analysis_reports.py` and `app/backend/tools/generate_signal_tracking_reports.py`.
  - The files were tracked in git, but they are report outputs rather than source or contract definitions.
  - No repo references were found to the concrete report filenames.

- `output/playwright/`
  - Playwright-run screenshots, logs, and local npm cache material.
  - These files are diagnostic outputs only and are not imported or consumed by app code.
  - No repo references were found to the concrete screenshot/log filenames.

- `.playwright-cli/`
  - Playwright CLI local session artifacts and snapshots.
  - This tree is generated by Playwright tooling and is not part of the app source tree.
  - No repo references were found outside Playwright's own generated files.

- `**/__pycache__/`
  - Python bytecode caches.
  - These were untracked/generated and safe to remove.

## Boundary / dependency notes

- Keep `docs/reports/` for now.
  - Some report files are cross-referenced by other report files, so wholesale deletion is not yet evidence-backed enough for this pass.
- Keep the generator scripts.
  - `scripts/generate_tracking_analysis_reports.py` and `app/backend/tools/generate_signal_tracking_reports.py` are the source of the analysis outputs above.

## Remaining risky candidates not removed

- `docs/reports/`
  - Needs a separate reference audit before any deletion.
- Other `output/` trees not reviewed in this pass.
  - Only the Playwright and analysis subtrees were treated as disposable artifacts here.

