# Champion Bad-Pick Ledger

## Purpose
This ledger is provisional. It is meant to find the largest loss bucket first, not to build a perfect taxonomy.

## Dominant Buckets
- `ranking_no_op`
- `regime_mismatch`
- `liquidity_trap`
- `weak_recovery`
- `breakout_failure`

## Read
- The current research problem is not lack of families.
- The bigger problem is that many variants do not move the top-K set at all.
- The strongest loss patterns are still bucketed too coarsely, so the next candidate must target one bucket at a time.

## Evidence
- `7806` is the clearest liquidity-trap example in the structural notes.
- `4684` and `2897` show a liquidity floor that is too weak to change the selected names.
- The monthly proxy studies repeatedly show regime-sensitive thresholds that are either inactive or too strict.

## Source
- `C:\work\meemee-screener\docs\analysis\market_structure_ledger.json`
